# Rename this file to .env and fill in your actual credentials.
# NEVER commit the actual .env file to version control.

# ===== API Keys & Credentials =====

# ASI-1 Mini API Key - Get your API key from https://innovationlab.fetch.ai/
# This is the API key for the ASI-1 Mini model provided by fetch.ai
ASI1_API_KEY=your_asi1_api_key_here

# OpenAI API Key (Required for decision making logic)
# This is OpenAI's API key, used to run GPT models for handling decision logic
OPENAI_API_KEY=your_openai_api_key_here

# Google Maps API Key (for location and restaurant data)
# Enable Places API in Google Cloud Console for this key
# Google Maps API key, used for searching restaurants and obtaining location information
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Aipolabs API Key (for enhanced restaurant information)
# Get your API key from https://aci.dev/
# Aipolabs API key, used for obtaining enhanced restaurant information
AIPOLABS_ACI_API_KEY=your_aipolabs_aci_api_key

# Aipolabs linked account ID (required for API calls)
# Aipolabs linked account ID, required for API calls
LINKED_ACCOUNT_OWNER_ID=your_linked_account_id

# ===== Feature Configuration Options =====

# Set to "true" to use ASI-1 Mini model, set to "false" to use OpenAI model
# This controls which LLM service provider the system uses for general natural language tasks
USE_ASI1=true

# Set to "true" to enable Aipolabs API integration, set to "false" to disable
# This controls whether to use TAVILY search to get additional restaurant information
USE_ACI_DEV=true

# Set to "true" to use Flock model for Web3 information, set to "false" to use OpenAI's GPT-4o-mini
# This controls how the system obtains information about restaurant Web3 features like crypto payments and NFT rewards
USE_FLOCK_MODEL=false

# Default model setting
# When USE_ASI1=true, use "asi1-mini"
# When USE_ASI1=false, use an OpenAI model name like "gpt-4o-mini" or "gpt-4o"
# This sets the base model used for natural language understanding in the system
DEFAULT_MODEL=asi1-mini

# Flock Model and OpenAI Settings
ALWAYS_ADD_WEB3_INFO=false
WEB3_MAX_RESTAURANTS=5
WEB3_REQUEST_TIMEOUT=10

# ===== File Path Configuration =====

# Log files directory path, the system will save runtime logs in this directory
# If the directory doesn't exist, the system will create it automatically
LOGS_DIR=logs 